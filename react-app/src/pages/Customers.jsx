import React from 'react';

const Customers = () => {
  return (
    <div>
      <h1 style={{ fontSize: '1.875rem', fontWeight: 'bold', color: 'var(--primary-color)' }}>
        Clients
      </h1>
      <p style={{ color: 'var(--text-muted)', marginTop: '0.5rem' }}>Gestion de la base client.</p>
    </div>
  );
};

export default Customers;
