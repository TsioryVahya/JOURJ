import React from 'react';

const Dashboard = () => {
  return (
    <div>
      <h1 style={{ fontSize: '1.875rem', fontWeight: 'bold', color: 'var(--primary-color)' }}>
        Tableau de bord
      </h1>
      <p style={{ color: 'var(--text-muted)', marginTop: '0.5rem' }}>Bienvenue dans votre espace d'administration.</p>
    </div>
  );
};

export default Dashboard;
